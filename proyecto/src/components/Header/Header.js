import Navbar from "../Navbar/Navbar";

const Header = (props) => {
    return (
        <div>
         
            <Navbar {...props} />
            
        </div>
    );
}

export default Header;