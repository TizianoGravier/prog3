import React from "react";
import VerMas from "../components/VerMas/VerMas";

const Mas = (props) => {
  return (
    <>
      <VerMas match={props.match} />
    </>
  );
};

export default Mas;